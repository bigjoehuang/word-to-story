# Supabase Configuration
# 在 Supabase Dashboard > Settings > API 中获取以下配置
# 参考文档: https://supabase.com/docs/guides/getting-started/quickstarts/nextjs
# 
# Project URL: 项目 URL（格式：https://xxxxx.supabase.co）
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
# 
# Publishable (anon) key: 用于客户端，可以公开，受 RLS (Row Level Security) 保护
# 在 Supabase Dashboard > Settings > API > Project API keys > anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_publishable_key
# 
# Secret (service_role) key: 用于服务端，必须保密，绕过 RLS
# ⚠️ 警告：此密钥具有完全访问权限，只能在服务端使用，绝不能暴露给客户端
# 在 Supabase Dashboard > Settings > API > Project API keys > service_role secret
SUPABASE_SERVICE_ROLE_KEY=your_supabase_secret_key

# DeepSeek API Configuration
# 在 DeepSeek 控制台获取 API Key
# 参考文档: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key

# 豆包大模型 API Configuration (for image generation)
# 使用Bearer token认证
DOUBAO_API_KEY=your_doubao_api_key
# 图片生成模型的endpoint ID（格式如：ep-20251203112650-nhdh4）
# 在火山引擎控制台创建图片生成endpoint后获取
DOUBAO_IMAGE_MODEL=ep-20251203112650-nhdh4

# 火山引擎 TTS API Configuration (for audio generation)
# 是否启用播客音频功能（设置为 true 启用，false 或不设置则禁用）
ENABLE_PODCAST_AUDIO=false
# 在火山引擎控制台 -> 豆包语音 -> 应用管理中创建应用，获取appid和access_token
# 参考文档: https://www.volcengine.com/docs/6561/1668014?lang=zh
VOLCENGINE_APP_ID=your_volcengine_app_id
VOLCENGINE_ACCESS_TOKEN=your_volcengine_access_token
# 可选：自定义TTS API endpoint（如果不设置，使用默认值）
# VOLCENGINE_TTS_ENDPOINT=https://openspeech.bytedance.com/api/v1/tts
# 可选：自定义集群名称（如果不设置，使用默认值volcano_tts）
# VOLCENGINE_CLUSTER=volcano_tts
# 可选：自定义语音类型（如果不设置，使用默认值BV700_streaming）
# VOLCENGINE_VOICE_TYPE=BV700_streaming
# 可选：自定义采样率（如果不设置，使用默认值24000）
# VOLCENGINE_SAMPLE_RATE=24000
# 可选：自定义语速（如果不设置，使用默认值1.0）
# VOLCENGINE_SPEED_RATIO=1.0
# 可选：自定义音量（如果不设置，使用默认值1.0）
# VOLCENGINE_VOLUME_RATIO=1.0
# 可选：自定义音调（如果不设置，使用默认值1.0）
# VOLCENGINE_PITCH_RATIO=1.0

# Daily Story Generation Limit
# 每日每个IP地址可以创作的故事数量限制
DAILY_STORY_LIMIT=5

